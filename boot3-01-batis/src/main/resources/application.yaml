spring:
    application:
        name: demo
    datasource:
        url: jdbc:mysql://localhost:3306/mydb?serverTimezone=Asia/Shanghai&userSSL=false
        username: root
        password: 123456
        driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
    configuration:
        log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

